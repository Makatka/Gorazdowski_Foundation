import styled from 'styled-components';

export const SectionHeaderStyles = styled.h2`
  margin-top: 0;
  margin-bottom: 50px;
  font-family: ${({ theme }) => theme.fonts.serifFont};
  font-size: 39px;
  line-height: 47px;
  font-weight: 500;
`;
